# LRU-Cache
This project demonstrates the implementation of an LRU (Least Recently Used) Cache, that efficiently stores and retrieves data while maintaining a fixed capacity. The cache automatically removes the least recently accessed items when the capacity is exceeded, ensuring optimal use of memory.
